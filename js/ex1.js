//Complete the following functions
const bodyNodes = () => {
  var nodes = document.body.childNodes;//get array of all the nodes of the body tag 
  nodes.forEach((node) => {
  console.log(node); //Displays all nodes
  });
  }
  bodyNodes();



